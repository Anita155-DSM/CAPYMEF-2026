# Frontend

Este frontend quedó arrancado con Vite + React.

## Lo que ya hicimos

- Instalamos `react-router-dom` y `axios`.
- Dejamos `BrowserRouter` configurado en `src/App.jsx`.
- La ruta raíz `/` muestra un mensaje de bienvenida simple.

## Arranque

- `npm install`
- `npm run dev`
